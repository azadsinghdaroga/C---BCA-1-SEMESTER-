#include <stdio.h>
int main()
{
    int marks = 75;

    if( marks >= 75) {
        
        printf("GRADE : A %d\n" , marks) ;
     }
     
     else if( marks >=60) {
        
        printf("GRADE : B %d\n" ,marks);
     }

     else{
        
        printf("GRADE : B %d\n" ,marks);
     }

     return 0;
}
